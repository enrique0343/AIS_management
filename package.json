{
  "name": "ais-management",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.33.4",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "pnpm --filter @ais/api dev",
    "dev:web": "pnpm --filter @ais/web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  }
}
